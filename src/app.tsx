import React from 'react';
import {TasksList} from "@/pages/TasksList/TasksList";

export const App = () => {
    return (
        <div className={'app'}>
            <TasksList/>
        </div>

    );
};
